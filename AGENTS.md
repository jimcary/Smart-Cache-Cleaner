# Project Instructions & Rules

- **Git Auto-Commit Requirement**: After making code edits or file modifications in any task, always automatically stage and commit all changes to Git before completing the turn:
  `git add -A && git commit -m "Auto-commit: <brief summary of changes>"`
